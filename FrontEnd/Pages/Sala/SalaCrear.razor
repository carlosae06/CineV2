@page "/Salas/crear"

@inject IRepositorio repositorioHTTP
@inject NavigationManager navigationManager
@inject IMostrarMensajes mostrarMensaje


<FormularioSala OnValidSubmit="@Guardar" model="@model"></FormularioSala>


@code {

    DTOs.Sala.SalaActualizarDTO model = new DTOs.Sala.SalaActualizarDTO();

    private async Task Guardar()
    {
        //var pregunta = await mostrarMensaje.Confirmacion("Insertar","¿Está seguro de guardar el autor?", "question");
        //if (pregunta)
        //{
        var response = await repositorioHTTP.Post<DTOs.Sala.SalaActualizarDTO>("Salas", model);
        //if (response.Error)
            //{
            //   await mostrarMensaje.MostrarMensajeError("Error al guardar el Categoria");
            //}
            //else
            //{
            //    await mostrarMensaje.MostrarMensajeExitoso("Categoria guardado con éxito");
            navigationManager.NavigateTo("/Salas");
        //    }
        //}

        
    }

}